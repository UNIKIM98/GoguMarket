<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goguma.biz.mapper.BizMemMapper">

	<!-- 가게목록 전체조회 -->
	<select id="getBizList" resultType="com.goguma.biz.vo.BizMemVO">
		SELECT m.*, d.*
		FROM biz_mem m LEFT OUTER JOIN biz_dangol d
						ON m.biz_no = d.biz_no
	</select>

	<!-- 개별조회(가게상세페이지) -->
	<select id="bizInfo" parameterType="String"
		resultType="com.goguma.biz.vo.BizMemVO">
		SELECT *
		FROM biz_mem
		WHERE biz_no = #{bizNo}
	</select>


	<!-- 가게목록 페이징 -->
	<select id="bizListPage"
		parameterType="com.goguma.biz.vo.BizSearchVO" resultType="BizMemVO">
		SELECT * FROM(SELECT rownum rn, a.* FROM(
						SELECT *
						FROM biz_mem
						<include refid="criteria"></include>
						ORDER BY biz_no
		<![CDATA[
		) a WHERE rownum <= #{last} ) b WHERE rn >= #{first}
		]]>
	</select>

	<!-- 반복 쿼리문 -->
	<sql id="criteria">
		<where>
			<if test="keyword != null and keyword != ''">
				<choose>
					<when test="searchType != null and searchType != ''">
						<choose>
							<when test="'bizNm'.equals(searchType)">
								AND biz_nm LIKE '%' || #{keyword}|| '%'
							</when>
						</choose>
					</when>
				</choose>
			</if>
		</where>
	</sql>

	<!-- 가게리스트 갯수 카운팅(페이징용) -->
	<select id="bizListCnt" parameterType="BizSearchVO"
		resultType="int">
		select count(*)
		from biz_mem
		<!-- <include refid="creiteria"></include> -->

	</select>

		<!-- 단골 카운팅 -->
<!-- 	<select id="dangolCnt" parameterType="String" resultType="int"> -->
<!-- 		select count(*) -->
<!-- 		from biz_dangol -->
<!-- 		where biz_no = #{bizNo} -->
<!-- 	</select> -->

	<!-- 가게정보 첨부파일 -->
	<select id="bizMemImg" resultType="com.goguma.biz.vo.BizMemVO">
		SELECT b.*,
		(SELECT atch_path
        	FROM atch a 
                WHERE b.atch_id = a.atch_id
                AND ROWNUM = 1) atch_path
		FROM biz_mem b
	</select>


</mapper>