<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goguma.sns.mapper.SnsMapper">
	<select id="selectSnsList" resultType="com.goguma.sns.vo.SnsVO">
		SELECT s.*
		,(SELECT ATCH_PATH FROM ATCH a WHERE s.ATCH_ID = a.ATCH_ID AND ROWNUM
		=1)atch_path
		FROM SNS s
	</select>

	<select id="selectSns" resultType="com.goguma.sns.vo.SnsVO">
		SELECT s.*
		FROM ATCH a JOIN SNS
		s
		ON a.atch_id = s.atch_id
		WHERE s.sns_no = #{snsNo}

	</select>

</mapper>