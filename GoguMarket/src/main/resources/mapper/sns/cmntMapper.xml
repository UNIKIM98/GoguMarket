<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goguma.sns.mapper.SnsCmntMapper">

	<select id="SelectCmntlist" parameterType="int"
		resultType="com.goguma.sns.vo.SnsCmntVO">
		SELECT * FROM sns_cmnt
		WHERE sns_no = #{snsNo}
		ORDER BY
		group_no,recmnt_order
	</select>


	<insert id="insertReply"
		parameterType="com.goguma.sns.vo.SnsCmntVO">
		<selectKey keyProperty="cmntNo" resultType="int"
			order="BEFORE">
			SELECT NVL(MAX(cmnt_no),0)+1 AS cmntNo
			FROM SNS_CMNT
		</selectKey>

		INSERT INTO SNS_CMNT (
		cmnt_no,
		sns_no,
		cmnt_cn,
		cmnt_mem,
		cmnt_ymd,
		group_no,
		recmnt_order,
		step

		)
		VALUES (
		#{cmntNo},
		#{snsNo},
		#{cmntCn},
		#{cmntMem},
		CURRENT_Date,
		<choose>
			<when test="groupNo == 0">#{groupNo} = null</when>
			<otherwise>#{groupNo}+1</otherwise>
		</choose>
		,
		<choose>
			<when test="recmntOrder == 0">#{recmntOrder} = null</when>
			<otherwise>#{recmntOrder}+1</otherwise>
		</choose>
		,
		<choose>
			<when test="step == 0">#{step} = null</when>
			<otherwise>#{step}+1</otherwise>
		</choose>
		)
	</insert>

	<update id="UpdaterecmntOrder" parameterType="com.goguma.sns.vo.SnsCmntVO">
		UPDATE sns_cmnt SET recmnt_order = recmnt_order +1 WHERE recmnt_order > #{recmntOrder}

	</update>




</mapper>